% A unit step that steps up from 0 to 1 at 0.03 seconds

for t = 0:numSteps
    
    
    
end

% A sinusoid, \(\sin(2 \pi f t)\), with f = 1/0.03 Hz and various
%     other frequencies

for t = 0:numSteps
    
    
    
end

% A gaussian pulse with a magnitude of 1 and std. dev. of 0.03
%     seconds and delay of 0.06 seconds.

for t = 0:numSteps
    
    
    
end
